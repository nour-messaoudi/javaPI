<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/8.0.171"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="tn.esprit.controllers.ModifierPostController"
      spacing="10"
      style="-fx-background-color: #f0f0f0;">

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <!-- En-tête -->
    <HBox alignment="CENTER" style="-fx-padding: 10; -fx-background-color: #3498db;">
        <Label text="Modifier Post" style="-fx-text-fill: white; -fx-font-size: 24px; -fx-font-weight: bold;"/>
    </HBox>

    <!-- Formulaire pour la modification du post -->
    <GridPane hgap="10" vgap="10">
        <Label text="Titre:" GridPane.columnIndex="0" GridPane.rowIndex="0"/>
        <TextField fx:id="titreField" GridPane.columnIndex="1" GridPane.rowIndex="0"/>

        <Label text="Contenu:" GridPane.columnIndex="0" GridPane.rowIndex="1"/>
        <TextArea fx:id="contenuField" GridPane.columnIndex="1" GridPane.rowIndex="1"
                  prefRowCount="5" wrapText="true"/>
    </GridPane>

    <!-- Boutons d'action -->
    <HBox spacing="10" alignment="CENTER_RIGHT" style="-fx-padding: 10;">
        <Button text="Annuler" onAction="#handleCancel" style="-fx-background-color: #e74c3c; -fx-text-fill: white;"/>
        <Button text="Mettre à jour" onAction="#handleUpdate" style="-fx-background-color: #2ecc71; -fx-text-fill: white;" defaultButton="true"/>
    </HBox>
</VBox>